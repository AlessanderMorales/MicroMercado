@page
@model MicroMercado.Presentation.Pages.NewCategoryModel

@{
    ViewData["Title"] = "Nueva Categoría";
    Layout = "~/Presentation/Pages/Shared/_Layout.cshtml";
}

<div class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1 class="m-0">Agregar Nueva Categoría</h1>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a asp-page="/Index">Inicio</a></li>
                    <li class="breadcrumb-item"><a asp-page="/CategoryPage">Categorías</a></li>
                    <li class="breadcrumb-item active">Nueva</li>
                </ol>
            </div>
        </div>
    </div>
</div>

<div class="content">
    <div class="container-fluid">
        @if (!string.IsNullOrEmpty(Model.SuccessMessage))
        {
            <div class="alert alert-success">@Model.SuccessMessage</div>
        }
        @if (!string.IsNullOrEmpty(Model.ErrorMessage))
        {
            <div class="alert alert-danger">@Model.ErrorMessage</div>
        }

        <div class="card shadow">
            <div class="card-header">
                <h3 class="card-title">Datos de la Categoría</h3>
            </div>
            <div class="card-body">
                <form method="post">
                    @Html.AntiForgeryToken()
                    <div class="mb-3">
                        <label asp-for="CreateCategory.Name" class="form-label">Nombre</label>
                        <input asp-for="CreateCategory.Name" class="form-control" />
                        <span asp-validation-for="CreateCategory.Name" class="text-danger"></span>
                    </div>
                    <div class="mb-3">
                        <label asp-for="CreateCategory.Description" class="form-label">Descripción</label>
                        <textarea asp-for="CreateCategory.Description" class="form-control" rows="3"></textarea>
                        <span asp-validation-for="CreateCategory.Description" class="text-danger"></span>
                    </div>
                    <button type="submit" class="btn btn-primary">Guardar</button>
                    <a asp-page="/CategoryPage" class="btn btn-secondary">Cancelar</a>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
